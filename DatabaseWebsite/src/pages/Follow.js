/*
import React, { useState, useEffect, useRef} from 'react';
import './Follow.css'
import { getUsername } from '../userData/user';
import { getUserId } from '../userData/user';
import { getFollowing } from '../userData/follows'
import { getFollowers } from '../userData/follows'
/*other imports?*/
/*
function Follow({}) {
    const userID = getUserID();
    const [following, setFollowing] = useState([]);

    // fetch following list from backend
    const fetchFollowing = async () => {
        const followingData = await getFollowing()

        if (followingData) {
            setFollowing(followingData); // add User's following to state
            console.log("Fetched following:", followingData);

            // get 

        }
    }

    return (

    )
}

export default Follow;
*/